name: build an push docker image push to AWS ECR with OIDC

on:
  push:
    branches: [main]
    paths: 
      - "docker/**"

jobs:
  build:
    name: Build the image and deploy it to AWS ECR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@62f4f872db3836360b72999f4b87f1ff13310f3a


      - name: Build, tag and push image to ECR
        id: build-image
        env: 
          PROJECT_NAME: ${{ vars.PROJECT_NAME }}
          REPO_NAME: ${{ vars.PROJECT_NAME }}-repo 
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
         docker buildx build --platform linux/amd64 \
          -t $PROJECT_NAME:$IMAGE_TAG \
          -f docker/Dockerfile \
         .

         docker tag $PROJECT_NAME:$IMAGE_TAG \
          $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG
        
         docker push $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG

